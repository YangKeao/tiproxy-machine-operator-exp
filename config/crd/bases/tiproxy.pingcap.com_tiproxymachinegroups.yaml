---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: tiproxymachinegroups.tiproxy.pingcap.com
spec:
  group: tiproxy.pingcap.com
  names:
    kind: TiProxyMachineGroup
    listKind: TiProxyMachineGroupList
    plural: tiproxymachinegroups
    shortNames:
    - tpmg
    singular: tiproxymachinegroup
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TiProxyMachineGroup defines a group of TiProxy machines managed
          by this operator.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              bootstrap:
                description: Bootstrap controls how machine-side agent startup context
                  is prepared.
                properties:
                  kubeconfig:
                    properties:
                      inline:
                        description: Inline is the full kubeconfig content written
                          directly to Path.
                        type: string
                      path:
                        type: string
                      remoteRef:
                        description: |-
                          RemoteRef points to an external store entry that contains kubeconfig content.
                          For AWS provider, backend "parameterStore" is treated as SSM Parameter Store.
                        properties:
                          backend:
                            type: string
                          identifier:
                            type: string
                        type: object
                    type: object
                  userData:
                    type: string
                type: object
              exposure:
                description: Exposure controls shared load balancer behavior.
                properties:
                  loadBalancers:
                    description: |-
                      LoadBalancers controls shared load balancer exposure endpoints.
                      Nil means one default internal load balancer named "internal".
                      An explicit empty list disables load balancer exposure.
                    items:
                      properties:
                        loadBalancerName:
                          description: LoadBalancerName optionally overrides the cloud
                            resource name for this load balancer.
                          type: string
                        name:
                          description: Name uniquely identifies this load balancer
                            inside one TiProxyMachineGroup.
                          type: string
                        scope:
                          description: |-
                            Scope controls whether the load balancer is internal or internet-facing.
                            Supported values: "internal", "external". Default: "internal".
                          type: string
                        subnetIDs:
                          description: |-
                            SubnetIDs optionally overrides which subnets host this load balancer.
                            If unset, the machine placement subnet set is reused.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                type: object
              linkSelector:
                description: LinkSelector optionally limits which links are considered.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              machine:
                description: Machine selects the machine image and size class.
                properties:
                  class:
                    type: string
                  identityRef:
                    description: |-
                      IdentityRef points to a provider-specific machine identity attachment.
                      On AWS this is currently interpreted as an IAM instance profile name or ARN.
                    type: string
                  image:
                    type: string
                  sshKeyName:
                    description: SSHKeyName requests provider-managed SSH key injection
                      when supported.
                    type: string
                type: object
              network:
                description: Network selects provider network attachments required
                  to place the machines.
                properties:
                  securityGroupIDs:
                    description: SecurityGroupIDs configures network policy attachments
                      where the provider supports them.
                    items:
                      type: string
                    type: array
                  subnetIDs:
                    items:
                      type: string
                    type: array
                type: object
              placement:
                description: Placement expresses the desired region and failure-domain
                  spread intent.
                properties:
                  failureDomains:
                    description: FailureDomains expresses preferred fault domains,
                      such as AZs.
                    items:
                      type: string
                    type: array
                  region:
                    description: Region selects the provider region that hosts this
                      machine group.
                    type: string
                  spreadPolicy:
                    description: |-
                      SpreadPolicy expresses desired spread behavior across failure domains.
                      Supported values are implementation-defined. "Balanced" is recommended.
                    type: string
                type: object
              portRange:
                description: PortRange controls frontend port allocation range for
                  TiProxyMachinePort resources.
                properties:
                  end:
                    format: int32
                    type: integer
                  start:
                    format: int32
                    type: integer
                required:
                - end
                - start
                type: object
              provider:
                description: Provider selects cloud implementation, e.g. "aws" or
                  "none".
                type: string
              providerConfigRef:
                description: ProviderConfigRef optionally points to provider-specific
                  defaults or advanced settings.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              replicas:
                description: Replicas controls desired machine count in the backing
                  machine group.
                format: int32
                type: integer
              resourceNames:
                description: ResourceNames optionally overrides provider resource
                  names generated for this machine group.
                properties:
                  group:
                    type: string
                  machineTemplate:
                    type: string
                type: object
              scaling:
                description: Scaling controls allowed capacity range for the backing
                  machine group.
                properties:
                  maxReplicas:
                    format: int32
                    type: integer
                  minReplicas:
                    format: int32
                    type: integer
                type: object
              tags:
                additionalProperties:
                  type: string
                description: Tags are copied to provider resources where supported.
                type: object
              tiproxy:
                description: TiProxy controls runtime configuration for the machine-side
                  agent.
                properties:
                  apiPort:
                    format: int32
                    type: integer
                  apiScheme:
                    type: string
                  baseImage:
                    type: string
                  certDir:
                    type: string
                  config:
                    description: Config keeps extra TiProxy config fields in a schemaless
                      form.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  configPath:
                    type: string
                  containerName:
                    type: string
                  extraArgs:
                    items:
                      type: string
                    type: array
                  image:
                    description: 'Deprecated: prefer baseImage + version.'
                    type: string
                  version:
                    type: string
                type: object
            required:
            - portRange
            type: object
          status:
            properties:
              allocatedPorts:
                additionalProperties:
                  format: int32
                  type: integer
                description: AllocatedPorts stores allocated frontend ports keyed
                  by TiProxyMachinePort UID.
                type: object
              cloud:
                properties:
                  frontendAddress:
                    description: 'Deprecated: use LoadBalancers.'
                    type: string
                  frontendID:
                    description: 'Deprecated: use LoadBalancers.'
                    type: string
                  loadBalancers:
                    items:
                      properties:
                        loadBalancerAddress:
                          type: string
                        loadBalancerID:
                          type: string
                        name:
                          type: string
                        scope:
                          type: string
                      type: object
                    type: array
                  machineGroupName:
                    type: string
                  machineTemplateID:
                    type: string
                  machineTemplateName:
                    type: string
                  machineTemplateVersion:
                    type: string
                type: object
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              machines:
                additionalProperties:
                  description: |-
                    MachineStatus captures runtime state reported by one machine-side agent.
                    The map key in TiProxyMachineGroup.status.machines is the machine identifier.
                  properties:
                    configHash:
                      type: string
                    lastHeartbeatTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    observedGeneration:
                      format: int64
                      type: integer
                    phase:
                      type: string
                    tiProxyImage:
                      type: string
                  type: object
                type: object
              observedGeneration:
                format: int64
                type: integer
              resolvedLinks:
                items:
                  properties:
                    clusterName:
                      type: string
                    configHash:
                      type: string
                    frontendUser:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    nsServerAddress:
                      type: string
                    pdAddresses:
                      items:
                        type: string
                      type: array
                    port:
                      format: int32
                      type: integer
                    routeNamespace:
                      type: string
                    tlsSecretRef:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    uid:
                      type: string
                  required:
                  - name
                  - namespace
                  - port
                  - uid
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
